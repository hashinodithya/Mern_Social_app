import React from "react";
import useStyles from './styles';

const Post =()=>{
    // eslint-disable-next-line
    const classes = useStyles();
    return (
        <h1>Post</h1>
    );
}

export default Post;